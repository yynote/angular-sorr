import {LineItemsSequenceHandler} from './line-items-sequence.helper';

describe('LineItemsSequenceHandler', () => {
  it('should create an instance', () => {
    expect(new LineItemsSequenceHandler()).toBeTruthy();
  });
});
