export enum ServiceStatusFilter {
  AllServices,
  ActiveServices,
  InactiveServices,
}

export enum ServiceCategoryType {
  AllCategories = -1,
  FullMetering,
  PartialMetering,
  PrepaidMetering,
  SingleTenant
}
