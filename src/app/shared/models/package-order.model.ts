export enum PackageOrder {
  Name = 1,
  CreateDate,
  ModifyDate,
  ServicesCount,
  Popularity
}

export enum PackageCategory {
  AllCategories = -1,
  FullMetering,
  PartialMetering,
  PrepaidMetering,
  SingleTenant
}
