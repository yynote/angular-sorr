import {EquipmentLogoSrcPipe} from './equipment-logo-src.pipe';

describe('EquipmentLogoSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentLogoSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
