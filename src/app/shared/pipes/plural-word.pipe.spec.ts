import {PluralWordPipe} from './plural-word.pipe';

describe('PluralWordPipe', () => {
  it('create an instance', () => {
    const pipe = new PluralWordPipe();
    expect(pipe).toBeTruthy();
  });
});
