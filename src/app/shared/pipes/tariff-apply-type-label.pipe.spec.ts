import {TariffApplyTypeLabelPipe} from './tariff-apply-type-label.pipe';

describe('TariffApplyTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new TariffApplyTypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
