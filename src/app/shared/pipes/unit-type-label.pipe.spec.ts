import {UnitTypeLabelPipe} from './unit-type-label.pipe';

describe('UnitTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitTypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
