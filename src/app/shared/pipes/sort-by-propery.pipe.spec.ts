import {SortByProperyPipe} from './sort-by-propery.pipe';

describe('SortByProperyPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByProperyPipe();
    expect(pipe).toBeTruthy();
  });
});
