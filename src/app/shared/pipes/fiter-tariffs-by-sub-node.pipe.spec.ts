import {FiterTariffsBySubNodePipe} from './fiter-tariffs-by-sub-node.pipe';

describe('FiterTariffsBySubNodePipe', () => {
  it('create an instance', () => {
    const pipe = new FiterTariffsBySubNodePipe();
    expect(pipe).toBeTruthy();
  });
});
