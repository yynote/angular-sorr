import {ParentMeterByGroupPipe} from './parent-meter-by-group.pipe';

describe('ParentMeterByGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new ParentMeterByGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
