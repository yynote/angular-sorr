import {ReadingStatusPipe} from './reading-status.pipe';

describe('ReadingStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ReadingStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
