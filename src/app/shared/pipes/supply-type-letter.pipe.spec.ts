import {SupplyTypeLetterPipe} from './supply-type-letter.pipe';

describe('SupplyTypeLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new SupplyTypeLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
