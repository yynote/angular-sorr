import {HasDuplicationFactorPipe} from './has-duplication-factor.pipe';

describe('HasDublicationFactorPipe', () => {
  it('create an instance', () => {
    const pipe = new HasDuplicationFactorPipe();
    expect(pipe).toBeTruthy();
  });
});
